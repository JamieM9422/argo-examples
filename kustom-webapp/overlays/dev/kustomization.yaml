apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
patches:
  - path: replicas.yaml
  - path: port.yaml

configMapGenerator:
  - envs:
      - config.properties
    name: mykustom-map

apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: guestbook
spec:
  generators:
  - list:
      elements:
      - namespace: engineering-dev
      - namespace: engineering-prod
      - namespace: finance-preprod
  template:
    metadata:
      name: '{{namespace}}-guestbook'
    spec:
      project: default
      source:
        repoURL: https://github.com/argoproj/argo-cd.git
        targetRevision: HEAD
        path: applicationset/examples/list-generator/guestbook/{{namespace}}
      destination:
        server: https://kubernetes.default.svc
        namespace: guestbook